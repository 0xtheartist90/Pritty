import BgVideo from '@/components/bg-video';

/**
 * Hero background video — chrome-free, no poster (the hero's dark background
 * covers the brief load so the video shows immediately).
 */
const HeroVideo = () => <BgVideo videoId='pluqPh8QSr0' start={10} />;

export default HeroVideo;
